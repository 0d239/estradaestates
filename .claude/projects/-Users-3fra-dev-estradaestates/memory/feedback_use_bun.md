---
name: Use bun everywhere
description: User prefers bun over npm for all package management and script running
type: feedback
---

Use `bun` instead of `npm` for all commands — installs, script runs, everything.

**Why:** User preference, project already configured with bun lockfile.

**How to apply:** Use `bun install`, `bun dev`, `bun run build`, `bun add <pkg>`, etc. Never suggest npm or yarn.
